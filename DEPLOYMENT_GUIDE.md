# SafeDrive AI - 배포 가이드

## 📱 Android 앱 배포 절차

이 문서는 SafeDrive AI 앱을 Google Play Store에 배포하기 위한 단계별 가이드입니다.

---

## ✅ 사전 준비 완료 항목

다음 설정이 이미 완료되었습니다:

- ✅ **Keystore 파일 생성**: `android/safedrive-release-key.jks`
- ✅ **서명 설정 구성**: `android/key.properties`
- ✅ **빌드 설정 완료**: `android/app/build.gradle`
- ✅ **ProGuard 규칙 설정**: `android/app/proguard-rules.pro`
- ✅ **.gitignore 업데이트**: 민감한 파일 제외

---

## 🔐 Keystore 정보

**⚠️ 중요: 이 정보를 안전하게 보관하세요!**

```
Keystore 파일: android/safedrive-release-key.jks
Store Password: safedrive2024
Key Alias: safedrive
Key Password: safedrive2024
유효기간: 10,000일 (약 27년)
```

> 💡 **주의사항:**
> - Keystore 파일과 비밀번호를 분실하면 앱 업데이트가 불가능합니다
> - 안전한 곳에 백업을 보관하세요
> - Git에는 절대 커밋하지 마세요 (이미 .gitignore에 추가됨)

---

## 📦 1단계: 앱 번들(AAB) 빌드

Google Play Store는 **Android App Bundle (AAB)** 형식을 권장합니다.

### 1.1 의존성 설치

```bash
cd /path/to/safety_copilot
flutter pub get
```

### 1.2 AAB 파일 생성

```bash
flutter build appbundle --release
```

빌드가 완료되면 다음 경로에 AAB 파일이 생성됩니다:
```
build/app/outputs/bundle/release/app-release.aab
```

### 1.3 빌드 확인

```bash
ls -lh build/app/outputs/bundle/release/
```

---

## 📲 2단계: APK 빌드 (선택사항)

테스트용 또는 직접 배포용 APK가 필요한 경우:

```bash
flutter build apk --release --split-per-abi
```

이 명령은 각 CPU 아키텍처별로 최적화된 APK를 생성합니다:
- `app-armeabi-v7a-release.apk` (구형 32bit 디바이스)
- `app-arm64-v8a-release.apk` (최신 64bit 디바이스)
- `app-x86_64-release.apk` (에뮬레이터/일부 태블릿)

생성 경로: `build/app/outputs/flutter-apk/`

---

## 🎯 3단계: Google Play Console 설정

### 3.1 Google Play Console 접속

1. [Google Play Console](https://play.google.com/console) 접속
2. Google 계정으로 로그인
3. "앱 만들기" 클릭

### 3.2 앱 기본 정보 입력

- **앱 이름**: SafeDrive AI
- **기본 언어**: 한국어
- **앱 유형**: 앱 및 게임
- **카테고리**: 자동차/교통

### 3.3 앱 콘텐츠 등급

1. 콘텐츠 등급 설문 작성
2. 앱 카테고리: 유틸리티
3. 광고 포함 여부 선택

### 3.4 개인정보처리방침

앱이 위치 정보 및 카메라를 사용하므로 개인정보처리방침 URL이 필요합니다.

**필수 포함 내용:**
- 위치 정보 수집 및 사용 목적
- 카메라 사용 목적
- 데이터 저장 방식 (로컬 저장)
- 제3자 제공 여부 (없음)

---

## 📤 4단계: AAB 업로드

### 4.1 프로덕션 트랙 생성

1. Play Console > 좌측 메뉴 > "프로덕션" 선택
2. "새 버전 만들기" 클릭
3. AAB 파일 업로드:
   ```
   build/app/outputs/bundle/release/app-release.aab
   ```

### 4.2 버전 정보 입력

```
버전 코드: 1
버전 이름: 1.0.0

출시 노트 (한국어):
• 얼굴 인식 기반 졸음 감지
• 휴대전화 사용 감지
• GPS 기반 급가속/급제동/급회전 감지
• 실시간 속도 표시
• 주행 기록 및 점수 분석
• 위험 상황 알림 (진동, 소리)
```

### 4.3 검토 및 출시

1. 모든 필수 항목 완료 확인
2. "검토" 클릭
3. "프로덕션으로 출시" 클릭

---

## 🖼️ 5단계: 스토어 등록 정보

### 5.1 필수 스크린샷

**휴대전화 스크린샷** (최소 2개, 권장 8개):
- 해상도: 1080 x 1920 이상
- 형식: PNG 또는 JPEG

**권장 스크린샷:**
1. 홈 화면
2. 모니터링 화면 (속도 표시)
3. 주행 기록 화면
4. 점수 및 통계 화면
5. 상세 이벤트 화면

### 5.2 앱 아이콘

- **크기**: 512 x 512 px
- **형식**: PNG (32bit)
- **투명도**: 없음

### 5.3 기능 그래픽

- **크기**: 1024 x 500 px
- **형식**: PNG 또는 JPEG

### 5.4 앱 설명

**짧은 설명 (80자 이하):**
```
AI 기반 운전 안전 모니터링 앱. 졸음, 휴대전화 사용, 위험 운전 행동 실시간 감지
```

**전체 설명 (최대 4000자):**
```
🚗 SafeDrive AI - 안전 운전의 새로운 기준

SafeDrive AI는 인공지능과 GPS 기술을 활용하여 운전 중 위험 상황을 실시간으로 감지하고 경고하는 스마트 안전 운전 보조 앱입니다.

✨ 주요 기능

📸 얼굴 인식 기반 안전 감지
• 졸음운전 실시간 감지 (눈 깜빡임, 하품, 고개 숙임)
• 휴대전화 사용 감지
• 3단계 위험도 경고 (주의/경고/위험)

🛰️ GPS 기반 운전 행동 분석
• 급가속 감지 및 경고
• 급제동 감지 및 경고
• 급회전 감지 및 경고
• 실시간 속도 표시
• 주행 거리 자동 기록

📊 상세한 운전 기록 및 분석
• 주행별 안전 점수 자동 계산
• S~F 등급제 평가
• 위험 이벤트 타임라인
• 최고/평균 속도 통계
• 주행 거리 기록

🔔 즉각적인 경고 시스템
• 위험 상황 시 진동 알림
• 경고음 재생
• 푸시 알림

🎯 사용법

1. 앱 설치 후 카메라 및 위치 권한 허용
2. "모니터링 시작" 버튼 터치
3. 휴대폰을 대시보드 거치대에 고정
4. 안전 운전 시작!

⚡ 앱이 자동으로:
- 얼굴을 모니터링하여 졸음 감지
- 휴대전화 사용 패턴 분석
- GPS로 운전 행동 추적
- 위험 상황 시 즉시 경고
- 모든 주행 기록 자동 저장

🏆 운전 점수 시스템

매 주행마다 안전 점수가 자동 계산됩니다:
• 기본 점수: 100점
• 졸음 감지: -5점
• 휴대전화 사용: -10점
• 급가속/급제동/급회전: 심각도별 -3~5점
• 장시간 안전 운전: +5점 보너스

📱 시스템 요구사항

• Android 8.0 (API 26) 이상
• 전면 카메라
• GPS 지원
• 최소 2GB RAM 권장

🔒 개인정보 보호

• 모든 데이터는 기기 내부에만 저장
• 서버 전송 없음
• 카메라는 얼굴 감지만 사용
• 위치 정보는 운전 행동 분석에만 활용

💡 사용 팁

• 밝은 환경에서 사용하세요 (얼굴 인식 정확도 향상)
• 휴대폰을 안정적으로 고정하세요
• 배터리 잔량 확인 (장시간 주행 시)
• 정기적으로 주행 기록을 확인하여 운전 습관 개선

⚠️ 주의사항

• 본 앱은 운전 보조 도구입니다
• 안전 운전의 책임은 운전자에게 있습니다
• 운전 중 앱 조작을 삼가세요
• 법적 요구사항을 준수하세요

🎁 완전 무료

SafeDrive AI는 광고 없이 완전 무료로 제공됩니다.
안전한 운전 문화를 만들어갑니다.

📧 문의 및 지원

버그 리포트 및 기능 제안:
support@safedrive.ai

---

SafeDrive AI와 함께 안전한 운전 되세요! 🚗💚
```

---

## 🧪 6단계: 테스트 (선택사항)

### 내부 테스트 트랙

프로덕션 출시 전 내부 테스터를 통해 검증하려면:

1. Play Console > "테스트" > "내부 테스트"
2. "새 버전 만들기"
3. AAB 업로드
4. 테스터 이메일 추가
5. 출시

---

## 🔄 7단계: 앱 업데이트

### 버전 업데이트 절차

1. **버전 코드 증가**:
   ```yaml
   # pubspec.yaml
   version: 1.0.1+2  # 1.0.1 = 버전 이름, 2 = 버전 코드
   ```

2. **변경사항 작성**:
   - 새로운 기능
   - 버그 수정
   - 성능 개선

3. **새 AAB 빌드**:
   ```bash
   flutter build appbundle --release
   ```

4. **Play Console 업로드**:
   - 프로덕션 > 새 버전 만들기
   - 새 AAB 업로드
   - 출시 노트 작성

---

## 📋 체크리스트

배포 전 최종 확인:

- [ ] Keystore 파일 안전하게 백업
- [ ] 앱 버전 확인 (pubspec.yaml)
- [ ] AAB 파일 빌드 성공
- [ ] 앱 이름 및 설명 확인
- [ ] 스크린샷 준비 (최소 2개)
- [ ] 앱 아이콘 준비 (512x512)
- [ ] 개인정보처리방침 URL 준비
- [ ] 콘텐츠 등급 완료
- [ ] 테스트 완료

---

## 🛠️ 문제 해결

### 빌드 실패 시

```bash
# 캐시 정리
flutter clean
flutter pub get

# 다시 빌드
flutter build appbundle --release
```

### 서명 오류 시

`key.properties` 파일 경로 확인:
```
android/key.properties
```

내용 확인:
```
storePassword=safedrive2024
keyPassword=safedrive2024
keyAlias=safedrive
storeFile=safedrive-release-key.jks
```

### ProGuard 오류 시

특정 라이브러리가 ProGuard로 인해 오류가 발생하면 `proguard-rules.pro`에 예외 추가:

```
-keep class 패키지명.** { *; }
```

---

## 📞 지원

추가 도움이 필요한 경우:

- [Flutter 공식 문서](https://docs.flutter.dev/deployment/android)
- [Google Play Console 가이드](https://support.google.com/googleplay/android-developer)

---

**작성일**: 2025-01-03
**버전**: 1.0
**대상**: SafeDrive AI v1.0.0
